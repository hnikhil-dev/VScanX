name: Release

on:
  push:
    tags:
      - 'v*.*.*'

# Explicit GITHUB_TOKEN permissions needed to create releases and upload artifacts
permissions:
  contents: write
  packages: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install build deps
        run: |
          python -m pip install --upgrade pip build
      - name: Build distributions
        run: |
          python -m build
      - name: Prepare release notes
        id: release_notes
        run: |
          body=$(cat CHANGELOG.md || true)
          echo "body<<EOF" >> $GITHUB_OUTPUT
          echo "$body" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
      - name: Create GitHub Release and upload artifacts
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          body: ${{ steps.release_notes.outputs.body }}
          files: dist/*
      # Optional: publish to PyPI (uncomment and set PYPI_API_TOKEN secret)
      # - name: Publish to PyPI
      #   uses: pypa/gh-action-pypi-publish@v1.6.0
      #   with:
      #     user: __token__
      #     password: ${{ secrets.PYPI_API_TOKEN }}
